# Backend & Networking
PORT=3000

# Database Configuration
POSTGRES_USER=your_user_here
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=tmg_db

# Note: The DATABASE_URL is constructed automatically in docker-compose.yml
# but if you run the server locally (outside Docker), you would use:
# DATABASE_URL=postgresql://your_user_here:your_password_here@localhost:5432/tmg_db
