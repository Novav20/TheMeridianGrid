# Backend & Networking
PORT=3000
NODE_ENV=development

# Database Configuration
POSTGRES_USER=your_user_here
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=tmg_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Note: The DATABASE_URL is constructed automatically in docker-compose.yml
# and in prisma.config.ts for local development using the variables above.

# Authentication
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=1h

# Seeding Configuration
# Set to 'true' to seed mock assets and telemetry
SEED_DUMMY=false