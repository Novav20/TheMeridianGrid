{
  "name": "the-meridian-grid",
  "private": true,
  "workspaces": [
    "tmg-server",
    "tmg-client",
    "tmg-shared"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev:client": "npm run dev -w tmg-client",
    "dev:server": "npm run dev -w tmg-server",
    "build:shared": "npm run build -w tmg-shared",
    "build:all": "npm run build --workspaces"
  }
}